@model VidlyPrototype.Models.ChangePasswordViewModel
@{
    ViewBag.Title = Resources.Controller_Users.change_password;
}

<section id="change-password-section">
    <div class="change-password-inner container inner-spacer-form">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card card-form p-3">
                    <div class="card-body">
                        <h3 class="display-4 border-bottom mb-3 py-1 text-center">@Resources.Controller_Users.change_password</h3>
                        @using (Html.BeginForm("ChangePassword", "Manage", FormMethod.Post, new { }))
                        {
                            @Html.AntiForgeryToken()
                            @Html.ValidationSummary("", new { @class = "text-danger" })
                            <div class="input-group mb-2">
                                <div class="input-group-prepend">
                                    <span class="input-group-text">
                                        <i class="fas fa-lock-open"></i>
                                    </span>
                                </div>
                                @Html.PasswordFor(m => m.OldPassword, new { @class = "form-control", placeholder = Resources.Controller_Users.current_password })
                            </div>
                            <div class="input-group mb-2">
                                <div class="input-group-prepend">
                                    <span class="input-group-text">
                                        <i class="fas fa-lock"></i>
                                    </span>
                                </div>
                                @Html.PasswordFor(m => m.NewPassword, new { @class = "form-control", placeholder = Resources.Controller_Users.new_password })
                            </div>
                            <div class="input-group mb-2">
                                <div class="input-group-prepend">
                                    <span class="input-group-text">
                                        <i class="fas fa-lock"></i>
                                    </span>
                                </div>
                                @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", placeholder = Resources.Controller_Users.confirm_password })
                            </div>
                            <button type="submit" class="btn btn-outline-primary btn-block">
                                @Resources.Controller_Users.change_password
                            </button>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}